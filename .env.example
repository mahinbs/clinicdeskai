# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES ONLY
# ============================================================================
# Copy this to .env and fill in your values
# Only VITE_ prefixed variables are exposed to the frontend

# Supabase Configuration (Safe to expose - RLS protects data)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_public_key_here

# App URL
VITE_APP_URL=http://localhost:5173

# ============================================================================
# BACKEND SECRETS - DO NOT PUT HERE!
# ============================================================================
# These belong in Supabase Dashboard → Edge Functions → Secrets
# 
# Set them via Supabase Dashboard or CLI:
# supabase secrets set RESEND_API_KEY=re_xxxxx
# supabase secrets set WHATSAPP_API_URL=https://api.whatsapp.com
# supabase secrets set WHATSAPP_API_TOKEN=your_token
#
# NEVER put SERVICE_ROLE_KEY in this file - it bypasses RLS!
# ============================================================================
